<%- include('../partials/header') %>

<div class="d-flex flex-wrap gap-3 justify-content-between align-items-center mb-4">
  <div>
    <h1 class="h3 mb-1">Patient List</h1>
    <p class="text-secondary mb-0">Search and manage patient records.</p>
  </div>
  <a href="/patients/new" class="btn btn-primary">Register Patient</a>
</div>

<div class="card border-0 shadow-sm mb-4">
  <div class="card-body">
    <form action="/patients" method="get" class="row g-2">
      <div class="col-lg-7">
        <label for="search" class="visually-hidden">Search patients</label>
        <input type="search" class="form-control" id="search" name="search" value="<%= search %>" placeholder="Search name, CNIC, contact, token, consultant, or address">
      </div>
      <div class="col-sm-6 col-lg-3">
        <label for="date" class="visually-hidden">Filter by date</label>
        <input type="date" class="form-control" id="date" name="date" value="<%= date %>">
      </div>
      <div class="col-sm-6 col-lg-2 d-flex gap-2">
        <button type="submit" class="btn btn-primary flex-grow-1">Search</button>
        <a href="/patients" class="btn btn-outline-secondary">Clear</a>
      </div>
    </form>
  </div>
</div>

<div class="card border-0 shadow-sm">
  <div class="table-responsive">
    <table class="table table-hover align-middle mb-0">
      <thead class="table-light">
        <tr>
          <th>Token</th><th>Patient Name</th><th>Age</th><th>Sex</th><th>CNIC</th>
          <th>Contact</th><th>Consultant</th><th>Type</th><th>Date</th><th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% if (!patients.length) { %>
          <tr><td colspan="10" class="text-center text-secondary py-5">No patients found.</td></tr>
        <% } %>
        <% patients.forEach((patient) => { %>
          <tr>
            <td><span class="badge text-bg-primary fs-6"><%= patient.tokenNumber %></span></td>
            <td class="fw-medium"><%= patient.patientName %></td>
            <td><%= patient.age %></td>
            <td><%= patient.sex %></td>
            <td class="text-nowrap"><%= formatCnic(patient.cnic) %></td>
            <td class="text-nowrap"><%= patient.contactNumber %></td>
            <td><%= patient.consultant ? patient.consultant.name : 'Deleted consultant' %></td>
            <td><%= patient.patientType %></td>
            <td class="text-nowrap"><%= patient.tokenDate %></td>
            <td>
              <div class="d-flex flex-wrap gap-1">
                <a href="/patients/<%= patient._id %>/token" class="btn btn-sm btn-outline-secondary">View</a>
                <a href="/patients/<%= patient._id %>/edit" class="btn btn-sm btn-outline-primary">Edit</a>
                <a href="/patients/<%= patient._id %>/token?print=1" class="btn btn-sm btn-outline-success">Print Token</a>
                <form action="/patients/<%= patient._id %>/delete" method="post" class="d-inline delete-form">
                  <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                </form>
              </div>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>

  <% if (totalPages > 1) { %>
    <div class="card-footer bg-white">
      <nav aria-label="Patient list pages">
        <ul class="pagination pagination-sm justify-content-center mb-0">
          <% for (let number = 1; number <= totalPages; number += 1) { %>
            <li class="page-item <%= number === page ? 'active' : '' %>">
              <a class="page-link" href="/patients?page=<%= number %>&search=<%= encodeURIComponent(search) %>&date=<%= encodeURIComponent(date) %>"><%= number %></a>
            </li>
          <% } %>
        </ul>
      </nav>
    </div>
  <% } %>
</div>

<%- include('../partials/footer') %>
